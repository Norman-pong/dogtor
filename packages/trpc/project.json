{
  "name": "@dogtor/trpc",
  "sourceRoot": "packages/trpc/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:trpc"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/trpc",
        "command": "bun run typecheck"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "packages/trpc",
        "command": "bun run build"
      }
    }
  }
}